%\tagged{Ans@ShortAns, Type@Compute, Topic@Limit, Sub@Rational, Sub@Indeterminant, Sub@Kevin}{


\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{10 \, x^{5} - 8 \, x^{2} + 3 \, x + 1}{\left({2 \, x^{24} - 3 \, x^{2} + 6 \, x + 8}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-7 \, x^{4} + 9 \, x^{2} - 9 \, x - 4}{\left({6 \, x^{16} - 9 \, x^{2} + 3}\right)^{\frac{1}{4}}} 
= \answer{-\frac{7}{6} \cdot 6^{\frac{3}{4}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-2 \, x^{4} + 2 \, x^{2} - 8 \, x - 2}{\left({9 \, x^{15} + 8 \, x^{2} + 2 \, x - 3}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{x^{5} - 3 \, x^{2} - 7}{\left({3 \, x^{20} + 2 \, x^{2} + 8 \, x + 10}\right)^{\frac{1}{4}}} 
= \answer{\frac{1}{3} \cdot 3^{\frac{3}{4}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{7 \, x^{7} - 8 \, x^{2} - 4}{\left({5 \, x^{9} + 3 \, x^{2} + 10 \, x + 4}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-2 \, x^{3} - 9 \, x^{2} + 5 \, x + 6}{\left({3 \, x^{30} - 10 \, x^{2} + 7 \, x - 6}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{8 \, x^{7} + 10 \, x^{2} + 8 \, x - 8}{\left({8 \, x^{16} - 10 \, x^{2} - 8 \, x - 3}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-4 \, x^{5} - 4 \, x^{2} + 10 \, x + 1}{\left({3 \, x^{18} + 10 \, x^{2} + 9 \, x - 4}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-9 \, x^{5} - 6 \, x^{2} + 5 \, x - 8}{\left({7 \, x^{12} - 5 \, x^{2} + 7}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{5} + 9 \, x^{2} + 3 \, x - 8}{\left({10 \, x^{20} - 4 \, x^{2} - 1}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-2 \, x^{3} + 9 \, x^{2} + 7 \, x - 8}{\left({8 \, x^{35} + 5 \, x^{2} - 6 \, x + 4}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-2 \, x^{4} - 10 \, x^{2} - 3 \, x - 7}{\left({x^{15} + 4 \, x^{2} - 8 \, x + 7}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-4 \, x^{4} - 9 \, x^{2} - 2 \, x + 2}{\left({10 \, x^{8} + 8 \, x^{2} - 9 \, x + 4}\right)^{\frac{1}{2}}} 
= \answer{-\frac{2}{5} \, \sqrt{10}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{10 \, x^{5} + 6 \, x^{2} - 8 \, x - 6}{\left({6 \, x^{20} - 9 \, x^{2} - 9 \, x - 6}\right)^{\frac{1}{5}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{4 \, x^{6} + x^{2} - 10 \, x + 2}{\left({6 \, x^{15} + 3 \, x^{2} + 4 \, x + 5}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-5 \, x^{7} - 4 \, x^{2} + 9 \, x + 10}{\left({4 \, x^{28} + 7 \, x^{2} + 7 \, x - 5}\right)^{\frac{1}{4}}} 
= \answer{\frac{5}{2} \, \sqrt{2}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{8 \, x^{3} - 3 \, x^{2} + 3 \, x}{\left({9 \, x^{12} + 7 \, x + 6}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-x^{6} + 4 \, x^{2} + 8}{\left({5 \, x^{14} - 3 \, x^{2} - x - 1}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-3 \, x^{5} - 5 \, x + 3}{\left({3 \, x^{20} + 5 \, x^{2} - 3 \, x}\right)^{\frac{1}{4}}} 
= \answer{-3^{\frac{3}{4}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-7 \, x^{7} - 8 \, x^{2} + 3 \, x + 9}{\left({8 \, x^{28} - 10 \, x^{2} - 6 \, x + 2}\right)^{\frac{1}{4}}} 
= \answer{-\frac{7}{8} \cdot 8^{\frac{3}{4}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{x^{7} - 6 \, x^{2} - 7 \, x - 8}{\left({3 \, x^{20} - 6 \, x^{2} + 8 \, x + 5}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{3} + 4 \, x^{2} - 6 \, x + 3}{\left({4 \, x^{12} - 6 \, x^{2} + 2 \, x - 2}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{10 \, x^{3} + 2 \, x^{2} + 5 \, x - 7}{\left({3 \, x^{16} + 8 \, x^{2} + 3 \, x - 8}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-5 \, x^{6} + 7 \, x^{2} + 5 \, x - 1}{\left({7 \, x^{24} + 5 \, x^{2} + 10 \, x + 4}\right)^{\frac{1}{4}}} 
= \answer{-\frac{5}{7} \cdot 7^{\frac{3}{4}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{3 \, x^{4} + 4 \, x^{2} + 3 \, x + 6}{\left({2 \, x^{14} + 10 \, x^{2} + 7 \, x - 9}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{6 \, x^{4} + 10 \, x^{2} - 7 \, x + 10}{\left({10 \, x^{12} - 7 \, x^{2} + x + 9}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{8 \, x^{7} + 2 \, x^{2} + 10 \, x - 6}{\left({x^{20} + 8 \, x^{2} - 4 \, x - 6}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-3 \, x^{3} - 4 \, x^{2} - 9 \, x - 1}{\left({10 \, x^{25} - 4 \, x^{2} + 9 \, x - 8}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-3 \, x^{7} - 6 \, x^{2} + 6 \, x}{\left({10 \, x^{10} + 6 \, x^{2} + 3 \, x - 4}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{3 \, x^{7} + 6 \, x^{2} + 9 \, x + 10}{\left({2 \, x^{12} - 5 \, x^{2} + 4}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-2 \, x^{7} + 9 \, x^{2} - 2 \, x - 7}{\left({6 \, x^{30} - 10 \, x^{2} + 4 \, x + 7}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{3} - 5 \, x^{2} - 5 \, x - 9}{\left({7 \, x^{15} - 5 \, x^{2} - x - 10}\right)^{\frac{1}{5}}} 
= \answer{7^{\frac{4}{5}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{5 \, x^{7} + 5 \, x^{2} + 2 \, x + 5}{\left({3 \, x^{12} + 3 \, x^{2} + 7 \, x - 7}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{4 \, x^{3} - 3 \, x^{2} + 3 \, x}{\left({5 \, x^{35} + 8 \, x^{2} - 7 \, x + 7}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-2 \, x^{6} + 4 \, x^{2} - 8 \, x - 3}{\left({3 \, x^{15} - 10 \, x^{2} + x + 4}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-x^{3} + x^{2} + 3 \, x - 9}{\left({8 \, x^{24} - 7 \, x^{2} + 3}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{4 \, x^{6} - x^{2} - 9 \, x - 2}{\left({3 \, x^{18} + 8 \, x^{2} + 3 \, x - 1}\right)^{\frac{1}{3}}} 
= \answer{\frac{4}{3} \cdot 3^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{4 \, x^{5} + 4 \, x^{2} - x}{\left({5 \, x^{18} + 5 \, x^{2} + 8 \, x - 9}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{7} + 3 \, x + 10}{\left({4 \, x^{9} - 6 \, x^{2} - 8 \, x - 10}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{2 \, x^{3} - 4 \, x^{2} - 4 \, x - 8}{\left({7 \, x^{15} - 4 \, x^{2} + 1}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{3 \, x^{7} + 4 \, x^{2} + 4 \, x - 10}{\left({x^{12} + 10 \, x^{2} - 3 \, x + 9}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-3 \, x^{4} - 5 \, x^{2} + 10 \, x + 6}{\left({4 \, x^{9} + 8 \, x^{2} - 6 \, x + 4}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{4 \, x^{3} + 5 \, x^{2} + 5 \, x - 7}{\left({8 \, x^{35} + 9 \, x^{2} - 1}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{x^{4} + 2 \, x^{2} + 2 \, x + 3}{\left({2 \, x^{30} + 8 \, x^{2} + 5 \, x - 9}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-4 \, x^{3} - 5 \, x^{2} - 2 \, x + 6}{\left({7 \, x^{21} + 5 \, x^{2} - 5 \, x + 1}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{7} + 5 \, x^{2} + 6 \, x - 10}{\left({7 \, x^{14} - x^{2} + 9 \, x - 2}\right)^{\frac{1}{2}}} 
= \answer{\sqrt{7}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{4} + 7 \, x^{2} + 8 \, x - 9}{\left({10 \, x^{8} - x^{2} + 3 \, x - 1}\right)^{\frac{1}{2}}} 
= \answer{\frac{3}{5} \, \sqrt{10}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-6 \, x^{4} + 2 \, x^{2} - 5 \, x + 5}{\left({9 \, x^{6} + 8 \, x^{2} + 4 \, x - 9}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-10 \, x^{4} + 8 \, x^{2} + 3 \, x - 9}{\left({2 \, x^{21} - 3 \, x^{2} - 7 \, x + 9}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{10 \, x^{3} + 2 \, x^{2} + x}{\left({10 \, x^{12} + 2 \, x + 10}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-4 \, x^{2} - 6 \, x + 3}{\left({6 \, x^{25} + x^{2} - 8 \, x - 1}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-6 \, x^{3} - 10 \, x^{2} + 2 \, x - 8}{\left({4 \, x^{9} + 7 \, x^{2} + 5 \, x + 8}\right)^{\frac{1}{3}}} 
= \answer{-\frac{3}{2} \cdot 4^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-2 \, x^{5} - 2 \, x^{2} - x - 6}{\left({2 \, x^{20} - 9 \, x^{2} + x + 7}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{x^{6} + x^{2} - 10 \, x - 9}{\left({8 \, x^{15} + 8 \, x + 7}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-9 \, x^{5} + 10 \, x^{2} - 9 \, x - 1}{\left({2 \, x^{6} - 7 \, x^{2} + 6 \, x - 7}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-7 \, x^{3} + 6 \, x^{2} - 6 \, x + 10}{\left({10 \, x^{10} - 10 \, x^{2} - 2 \, x - 9}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-7 \, x^{4} - 6 \, x^{2} + 10 \, x - 1}{\left({x^{30} - 8 \, x^{2} - 2 \, x + 6}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{x^{7} - 6 \, x^{2} + 6 \, x - 4}{\left({3 \, x^{6} + 3 \, x^{2} - x - 6}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{7} - 9 \, x^{2} - 5 \, x + 2}{\left({x^{18} - 6 \, x^{2} - 2 \, x + 3}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-2 \, x^{5} + 5 \, x^{2} - 10 \, x + 7}{\left({x^{24} - 10 \, x^{2} - 6 \, x - 6}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-9 \, x^{3} + 10 \, x^{2} + 2 \, x + 8}{\left({6 \, x^{25} - 7 \, x^{2} + 7 \, x + 1}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-3 \, x^{7} - 3}{\left({6 \, x^{21} + 10 \, x^{2} - 6 \, x - 7}\right)^{\frac{1}{3}}} 
= \answer{-\frac{1}{2} \cdot 6^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-6 \, x^{6} + 10 \, x^{2} + 7 \, x}{\left({x^{35} - 8 \, x^{2} - 7 \, x - 1}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-10 \, x^{4} + 6 \, x - 7}{\left({3 \, x^{15} - 9 \, x - 4}\right)^{\frac{1}{5}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-7 \, x^{3} + 3 \, x^{2} - 3}{\left({3 \, x^{15} + 7 \, x^{2} + 8 \, x - 4}\right)^{\frac{1}{5}}} 
= \answer{-\frac{7}{3} \cdot 3^{\frac{4}{5}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{x^{7} + 5 \, x^{2} + 10 \, x - 7}{\left({x^{15} + 9 \, x^{2} - 3 \, x - 4}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-8 \, x^{7} - 3 \, x^{2} + 3 \, x - 10}{\left({9 \, x^{12} - 8 \, x^{2} - 8}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-3 \, x^{5} + 3 \, x^{2} - 5 \, x - 4}{\left({7 \, x^{20} - 3 \, x^{2} - 9 \, x + 2}\right)^{\frac{1}{5}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-x^{6} + 7 \, x^{2} + 7 \, x + 6}{\left({3 \, x^{20} + x^{2} + 7 \, x + 9}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{10 \, x^{7} + 2 \, x^{2} - 9 \, x + 7}{\left({6 \, x^{8} + 3 \, x^{2} + 3 \, x - 4}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-3 \, x^{6} + 3 \, x^{2} + x - 10}{\left({x^{12} + 8 \, x^{2} - 6 \, x - 3}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{5} - 10 \, x^{2} + 5 \, x - 10}{\left({x^{14} + 5 \, x^{2} - 4}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-7 \, x^{7} + 7 \, x^{2} + 9 \, x - 5}{\left({9 \, x^{16} + 8 \, x^{2} + 7 \, x + 9}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{7} + 2 \, x^{2} - 9 \, x - 5}{\left({x^{24} - 5 \, x^{2} + 6 \, x - 9}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{5} - 4 \, x^{2} - x - 9}{\left({3 \, x^{20} - 3 \, x^{2} - x - 7}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{5 \, x^{5} + x^{2} - 10 \, x + 2}{\left({2 \, x^{15} + 4 \, x^{2} + 8 \, x - 5}\right)^{\frac{1}{5}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-5 \, x^{7} - 8}{\left({7 \, x^{21} - 3 \, x^{2} + 6 \, x + 10}\right)^{\frac{1}{3}}} 
= \answer{-\frac{5}{7} \cdot 7^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{x^{4} + 3 \, x^{2} - 7 \, x + 6}{\left({5 \, x^{12} + 7 \, x^{2} - 5 \, x + 3}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{5 \, x^{5} + x^{2} - 7 \, x - 1}{\left({9 \, x^{30} + 8 \, x^{2} - 7 \, x}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{9 \, x^{2} + 4 \, x - 9}{\left({10 \, x^{35} - 3}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-2 \, x^{4} + 9 \, x^{2} + x + 10}{\left({3 \, x^{15} - 8 \, x^{2} + 3 \, x - 3}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{2 \, x^{6} + 2}{\left({5 \, x^{12} - 9 \, x - 6}\right)^{\frac{1}{2}}} 
= \answer{\frac{2}{5} \, \sqrt{5}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{9 \, x^{4} + 7 \, x^{2} - 4 \, x - 2}{\left({6 \, x^{20} - 2 \, x^{2} + 8 \, x + 8}\right)^{\frac{1}{5}}} 
= \answer{\frac{3}{2} \cdot 6^{\frac{4}{5}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-10 \, x^{2} + 6 \, x + 10}{\left({8 \, x^{30} + 2 \, x^{2} + 9 \, x + 2}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{8 \, x^{2} - 8 \, x - 6}{\left({10 \, x^{18} + 7 \, x^{2} + 3 \, x - 1}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-x^{4} - x^{2} - 6 \, x + 9}{\left({5 \, x^{15} - 10 \, x^{2} + 8 \, x + 4}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-7 \, x^{7} - 8 \, x^{2} + 10 \, x - 5}{\left({x^{24} + 8 \, x^{2} + 4 \, x + 8}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-3 \, x^{5} - 4 \, x^{2} + 8 \, x - 3}{\left({8 \, x^{30} - 3 \, x^{2} - 10 \, x + 10}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{3 \, x^{4} - 4 \, x^{2} + 4 \, x + 5}{\left({4 \, x^{18} - 3 \, x^{2} - 4 \, x - 5}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{6} + 7 \, x^{2} + 9 \, x - 4}{\left({10 \, x^{25} - 9 \, x^{2} - 9 \, x - 7}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-3 \, x^{5} + 4 \, x^{2} - 6 \, x - 7}{\left({2 \, x^{35} - 5 \, x^{2} - 7 \, x - 7}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{4 \, x^{4} - x^{2} + 10 \, x + 7}{\left({x^{25} + 6 \, x^{2} - 6 \, x + 8}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{5 \, x^{6} + 10 \, x^{2} + 4 \, x + 5}{\left({7 \, x^{20} - x^{2} - 5 \, x - 7}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-2 \, x^{3} + x^{2} - 8 \, x}{\left({3 \, x^{24} - 8 \, x^{2} - 3 \, x + 1}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-7 \, x^{5} - 10 \, x^{2} + 3 \, x - 10}{\left({10 \, x^{35} - 4 \, x^{2} + 3 \, x + 4}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{2 \, x^{6} - 9 \, x^{2} + 4 \, x - 4}{\left({4 \, x^{28} + 9 \, x^{2} - 3 \, x + 7}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{6} + 10 \, x^{2} - 5 \, x + 7}{\left({2 \, x^{30} + 2 \, x^{2} + 2 \, x - 4}\right)^{\frac{1}{5}}} 
= \answer{\frac{7}{2} \cdot 2^{\frac{4}{5}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-3 \, x^{3} - 8 \, x^{2} - 3 \, x + 4}{\left({10 \, x^{24} + 8 \, x^{2} - 6 \, x - 8}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-4 \, x^{7} + 2 \, x^{2} - 5 \, x + 9}{\left({2 \, x^{24} + 10 \, x^{2} - 5 \, x + 7}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-x^{7} + x^{2} - 8 \, x + 2}{\left({8 \, x^{25} - 9 \, x^{2} - 6 \, x}\right)^{\frac{1}{5}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{2 \, x^{6} - 5 \, x^{2} + 4 \, x + 9}{\left({5 \, x^{10} - x^{2} + 7 \, x - 8}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{6} + x^{2} + x - 6}{\left({6 \, x^{30} + 9 \, x^{2} - 5 \, x - 5}\right)^{\frac{1}{5}}} 
= \answer{6^{\frac{4}{5}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{7 \, x^{6} + 2 \, x^{2} - 4 \, x - 5}{\left({8 \, x^{28} + 10 \, x^{2} + 3 \, x + 7}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{5} - 2 \, x^{2} + 7 \, x + 3}{\left({2 \, x^{20} + 9 \, x^{2} + 9 \, x + 1}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-4 \, x^{6} + 10 \, x^{2} + 4 \, x}{\left({4 \, x^{15} + 10 \, x^{2} + 5 \, x + 5}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-5 \, x^{6} - x^{2} - 3 \, x - 4}{\left({8 \, x^{12} - 4 \, x^{2} - x - 9}\right)^{\frac{1}{2}}} 
= \answer{-\frac{5}{4} \, \sqrt{2}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{2 \, x^{5} + x - 3}{\left({6 \, x^{6} - 2 \, x^{2} + 8 \, x - 9}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{6 \, x^{6} - 4 \, x^{2} - 9 \, x - 5}{\left({9 \, x^{15} + 3 \, x^{2} - 5 \, x + 4}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-x^{3} + 9 \, x^{2} + 3 \, x + 7}{\left({x^{12} - 6 \, x^{2} - 3 \, x + 9}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{4} + 6 \, x^{2} - 9 \, x - 5}{\left({2 \, x^{35} - 4 \, x^{2} - 7 \, x + 9}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{x^{4} + 7 \, x^{2} - x + 8}{\left({7 \, x^{15} + 6 \, x^{2} + 2 \, x}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{2 \, x^{4} - 4 \, x^{2} + 2 \, x}{\left({6 \, x^{18} - 6 \, x^{2} + 3 \, x - 6}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-6 \, x^{7} - 4 \, x^{2} - 3 \, x + 4}{\left({4 \, x^{28} - x^{2} - 4 \, x + 1}\right)^{\frac{1}{4}}} 
= \answer{-3 \, \sqrt{2}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{9 \, x^{5} + 9 \, x^{2} + 2 \, x - 9}{\left({8 \, x^{12} - 8 \, x^{2} + 2 \, x - 1}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-4 \, x^{3} + 6 \, x^{2} + 2 \, x + 7}{\left({7 \, x^{35} - x^{2} - 2 \, x + 2}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{8 \, x^{4} - 8 \, x^{2} + 9 \, x + 6}{\left({5 \, x^{6} + 4 \, x^{2} - 6 \, x + 1}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-10 \, x^{7} - x^{2} - x - 7}{\left({10 \, x^{15} - 2 \, x^{2} + 7 \, x - 1}\right)^{\frac{1}{5}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{x^{2} - 7 \, x + 5}{\left({3 \, x^{10} + 2 \, x^{2} + 3 \, x - 8}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{2 \, x^{7} + 3 \, x^{2} - 2 \, x - 8}{\left({x^{20} + 7 \, x^{2} + x - 4}\right)^{\frac{1}{5}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-10 \, x^{5} + 6 \, x^{2} + 7}{\left({x^{14} + 8 \, x^{2} - 2 \, x - 6}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-7 \, x^{4} + x^{2} - 5 \, x - 4}{\left({10 \, x^{14} - 3 \, x^{2} + 4 \, x + 3}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{10 \, x^{4} + 2 \, x}{\left({9 \, x^{20} + 5 \, x^{2} - 5 \, x - 10}\right)^{\frac{1}{5}}} 
= \answer{\frac{10}{9} \cdot 9^{\frac{4}{5}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-5 \, x^{5} + 4 \, x^{2} + x - 8}{\left({6 \, x^{30} + 2 \, x^{2} + 2 \, x - 6}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-3 \, x^{2} + 6 \, x - 3}{\left({9 \, x^{12} - 2 \, x^{2} - 8 \, x - 6}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-7 \, x^{4} + 6 \, x^{2} - 8 \, x + 1}{\left({8 \, x^{20} + 6 \, x^{2} - 2}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{7} - x^{2} + 10 \, x - 6}{\left({7 \, x^{15} + 2 \, x^{2} + 9 \, x - 5}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-x^{7} - 4 \, x^{2} - 6 \, x - 2}{\left({x^{30} - x^{2} - 7 \, x + 10}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{3 \, x^{6} - 7 \, x^{2} + 9 \, x - 6}{\left({4 \, x^{25} - 10 \, x^{2} - 4 \, x - 3}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-5 \, x^{6} + x^{2} + 3 \, x + 2}{\left({4 \, x^{12} - 6 \, x^{2} - x - 1}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{3 \, x^{6} + 3 \, x^{2} + 8 \, x - 10}{\left({2 \, x^{12} - 3 \, x^{2} - 8 \, x - 7}\right)^{\frac{1}{2}}} 
= \answer{\frac{3}{2} \, \sqrt{2}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{9 \, x^{5} + x^{2} + 4}{\left({5 \, x^{21} + x - 10}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-5 \, x^{7} + 6 \, x^{2} + 5 \, x + 1}{\left({6 \, x^{12} + 9 \, x^{2} + x - 3}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{8 \, x^{3} + 3 \, x^{2} - 2 \, x - 8}{\left({6 \, x^{8} + 8 \, x^{2} + 10 \, x + 10}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{7 \, x^{7} + 6 \, x^{2} - 6 \, x}{\left({7 \, x^{18} - 5 \, x^{2} - 10 \, x - 1}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-4 \, x^{2} - 4 \, x - 5}{\left({x^{12} + x^{2} - 6 \, x + 1}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{4 \, x^{5} + 4 \, x^{2} + 8 \, x - 4}{\left({8 \, x^{14} + 9 \, x^{2} - 4 \, x - 7}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-5 \, x^{5} - 7 \, x^{2} - 2 \, x + 9}{\left({6 \, x^{8} - 8 \, x^{2} + x + 2}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-2 \, x^{5} + x^{2} + 7 \, x - 2}{\left({3 \, x^{30} + 6 \, x^{2} + 9 \, x - 1}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-6 \, x^{7} - 10 \, x^{2} + 7 \, x - 3}{\left({2 \, x^{25} - 5 \, x^{2} + x}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{9 \, x^{5} + 6 \, x^{2} + 7 \, x - 4}{\left({6 \, x^{25} - 7 \, x^{2} + 10 \, x + 8}\right)^{\frac{1}{5}}} 
= \answer{\frac{3}{2} \cdot 6^{\frac{4}{5}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-x^{5} - 10 \, x^{2} - 2 \, x - 3}{\left({10 \, x^{8} + 2 \, x^{2} + 4 \, x + 2}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{3 \, x^{5} + 7 \, x^{2} - 5 \, x + 6}{\left({6 \, x^{12} + 3 \, x^{2} - 9 \, x + 1}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{6 \, x^{5} - 7 \, x^{2} - 9 \, x + 2}{\left({8 \, x^{35} + 6 \, x^{2} + 3}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{8 \, x^{6} + 9 \, x^{2} - x - 8}{\left({7 \, x^{24} - 8 \, x^{2} - x - 2}\right)^{\frac{1}{4}}} 
= \answer{\frac{8}{7} \cdot 7^{\frac{3}{4}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{8 \, x^{3} - 6 \, x^{2} + 2 \, x + 6}{\left({2 \, x^{12} + 5 \, x^{2} + 2 \, x + 9}\right)^{\frac{1}{4}}} 
= \answer{-\left(4 \cdot 2^{\frac{3}{4}}\right)}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-8 \, x^{6} - 3 \, x^{2} - 3 \, x - 9}{\left({2 \, x^{18} + 2 \, x^{2} + 3 \, x - 8}\right)^{\frac{1}{3}}} 
= \answer{-\left(4 \cdot 2^{\frac{2}{3}}\right)}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-3 \, x^{7} + 2 \, x^{2} - 4 \, x + 9}{\left({2 \, x^{12} - 6 \, x^{2} - 10 \, x + 4}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{3} - 4 \, x^{2} + 5 \, x - 8}{\left({x^{30} - 3 \, x^{2} - 3 \, x + 7}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-10 \, x^{4} + 3 \, x^{2} - 9 \, x}{\left({3 \, x^{35} - 6 \, x^{2} - 7 \, x + 10}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-5 \, x^{3} + x^{2} + 9 \, x - 3}{\left({2 \, x^{24} - 8 \, x^{2} + 5 \, x - 8}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{4} - 10 \, x^{2} + 5 \, x + 2}{\left({2 \, x^{12} - 9 \, x^{2} - 4 \, x - 9}\right)^{\frac{1}{3}}} 
= \answer{3 \cdot 2^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{4 \, x^{6} + 7 \, x^{2} - 9 \, x - 9}{\left({4 \, x^{30} + 2 \, x^{2} + 9}\right)^{\frac{1}{5}}} 
= \answer{4^{\frac{4}{5}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-8 \, x^{3} - 4 \, x^{2} - x + 1}{\left({10 \, x^{28} + 7 \, x^{2} - 2 \, x - 4}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-10 \, x^{5} + 6 \, x^{2} + 3 \, x + 8}{\left({2 \, x^{28} - 3 \, x^{2} - 10 \, x + 9}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-8 \, x^{3} + 4 \, x^{2} + 7 \, x + 2}{\left({10 \, x^{14} + x^{2} - 2 \, x + 10}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{9 \, x^{7} - 3 \, x^{2} + 4 \, x + 1}{\left({8 \, x^{35} - 9 \, x^{2} + 5 \, x + 1}\right)^{\frac{1}{5}}} 
= \answer{\frac{9}{8} \cdot 8^{\frac{4}{5}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{2 \, x^{4} + 5 \, x^{2} + 4 \, x - 6}{\left({10 \, x^{8} + 3 \, x^{2} + 2 \, x - 3}\right)^{\frac{1}{2}}} 
= \answer{\frac{1}{5} \, \sqrt{10}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-10 \, x^{7} - 9 \, x^{2} - 8 \, x + 1}{\left({9 \, x^{6} - 4 \, x^{2} - 10 \, x - 9}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-x^{3} + 2 \, x^{2} - 5 \, x + 9}{\left({10 \, x^{12} - 2 \, x^{2} + 3 \, x}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-4 \, x^{5} - 6 \, x^{2} - 6 \, x - 3}{\left({5 \, x^{35} + 7 \, x^{2} + 10 \, x + 3}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-x^{6} - 5 \, x^{2} - 5 \, x + 7}{\left({9 \, x^{12} - 10 \, x^{2} - 7 \, x - 3}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-10 \, x^{6} - 2 \, x^{2} - 8 \, x - 2}{\left({3 \, x^{10} - 3 \, x^{2} + 2 \, x + 10}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{2 \, x^{3} + x^{2} + 4 \, x + 1}{\left({10 \, x^{12} + 4 \, x^{2} - 10 \, x - 9}\right)^{\frac{1}{4}}} 
= \answer{-\frac{1}{5} \cdot 10^{\frac{3}{4}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-2 \, x^{6} - 9 \, x^{2} + 4 \, x - 2}{\left({3 \, x^{10} - 5 \, x^{2} + 7 \, x + 9}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{3} - 9 \, x^{2} - 2 \, x - 7}{\left({6 \, x^{12} - 9 \, x^{2} - x + 8}\right)^{\frac{1}{4}}} 
= \answer{\frac{7}{6} \cdot 6^{\frac{3}{4}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-4 \, x^{4} + 4 \, x^{2} + 2 \, x + 6}{\left({10 \, x^{14} - x^{2} + 4 \, x + 5}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{4 \, x^{5} - 3 \, x^{2} - 7 \, x + 10}{\left({6 \, x^{12} + 4 \, x^{2} - x - 1}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{8 \, x^{6} + 9 \, x^{2} + x - 8}{\left({9 \, x^{15} - 2 \, x^{2} + 9 \, x + 5}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{7} - x^{2} - 3 \, x + 9}{\left({3 \, x^{18} - 4 \, x^{2} + 6 \, x + 4}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-3 \, x^{3} - 2 \, x^{2} + 4 \, x + 8}{\left({4 \, x^{8} - 7 \, x^{2} - 6 \, x + 3}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-2 \, x^{6} - 5 \, x^{2} - 5 \, x - 4}{\left({7 \, x^{14} - 3 \, x^{2} + 5 \, x - 10}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{10 \, x^{7} + x^{2} + 10 \, x - 8}{\left({2 \, x^{8} + 5 \, x^{2} + 5 \, x + 10}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{4 \, x^{7} - x^{2} - 2 \, x - 5}{\left({9 \, x^{30} - x^{2} + 7 \, x - 1}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{3 \, x^{5} + 6 \, x^{2} + 8 \, x + 10}{\left({9 \, x^{16} + 5 \, x^{2} + 2 \, x - 5}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-3 \, x^{4} + 5 \, x^{2} + 8 \, x + 9}{\left({7 \, x^{12} + 5 \, x^{2} + 4 \, x - 10}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-10 \, x^{5} - 6 \, x^{2} - 8 \, x + 3}{\left({5 \, x^{12} - 7 \, x^{2} - 10 \, x + 6}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-7 \, x^{6} - x^{2} + 4}{\left({2 \, x^{12} + 9 \, x^{2} + 4 \, x - 2}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-4 \, x^{3} - 2 \, x^{2} - 6 \, x - 10}{\left({3 \, x^{18} - 8 \, x^{2} + x - 9}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{10 \, x^{3} - 10 \, x^{2} - 4 \, x - 9}{\left({7 \, x^{8} - 2 \, x^{2} - 5 \, x + 3}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{9 \, x^{6} - 5 \, x^{2} + 10 \, x - 9}{\left({6 \, x^{15} + 6 \, x^{2} + 5 \, x - 5}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-5 \, x^{4} - 4 \, x^{2} - 5 \, x - 8}{\left({7 \, x^{8} + 5 \, x^{2} + 3 \, x + 10}\right)^{\frac{1}{2}}} 
= \answer{-\frac{5}{7} \, \sqrt{7}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{x^{7} + 4 \, x^{2} + 10 \, x}{\left({x^{20} + 3 \, x^{2} - 6 \, x - 4}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{x^{3} + 8 \, x^{2} - 6 \, x - 3}{\left({6 \, x^{12} + 10 \, x^{2} + 6 \, x}\right)^{\frac{1}{4}}} 
= \answer{-\frac{1}{6} \cdot 6^{\frac{3}{4}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-6 \, x^{3} - 9 \, x^{2} - 5 \, x - 5}{\left({6 \, x^{14} + x^{2} - x + 8}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-x^{6} - x^{2} + 4 \, x - 1}{\left({10 \, x^{6} + 3 \, x^{2} + 2 \, x + 7}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-4 \, x^{7} - 3 \, x + 2}{\left({8 \, x^{12} - 5 \, x^{2} + 10}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{4 \, x^{7} - 7 \, x^{2} - 5 \, x + 3}{\left({6 \, x^{6} + 2 \, x^{2} + 6 \, x + 1}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{10 \, x^{7} - 2 \, x^{2} - 9 \, x - 3}{\left({x^{35} - 5 \, x^{2} + 7 \, x + 10}\right)^{\frac{1}{5}}} 
= \answer{10}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-8 \, x^{3} - 10 \, x^{2} - 8 \, x + 9}{\left({2 \, x^{12} + 5 \, x^{2} + 5 \, x}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{8 \, x^{7} + 2 \, x^{2} - x - 9}{\left({6 \, x^{24} - 10 \, x^{2} + 4 \, x + 7}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{10 \, x^{6} - 10 \, x^{2} - 2 \, x - 9}{\left({10 \, x^{28} - 3 \, x^{2} - 2 \, x + 7}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-5 \, x^{6} - 10 \, x^{2} + 10 \, x - 2}{\left({6 \, x^{12} - 8 \, x^{2} + 2 \, x + 3}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-6 \, x^{4} - 7 \, x^{2} - 10 \, x - 1}{\left({10 \, x^{35} + 5 \, x^{2} - 4 \, x + 8}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{4 \, x^{5} - 4 \, x^{2} - 7 \, x}{\left({2 \, x^{10} + 7 \, x^{2} - 8 \, x - 6}\right)^{\frac{1}{2}}} 
= \answer{-\left(2 \, \sqrt{2}\right)}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{10 \, x^{5} + x^{2} + x - 10}{\left({x^{12} + 7 \, x^{2} + 3}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{10 \, x^{3} - 10 \, x^{2} + 3 \, x - 1}{\left({3 \, x^{9} - 8 \, x^{2} - 6 \, x + 10}\right)^{\frac{1}{3}}} 
= \answer{\frac{10}{3} \cdot 3^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-9 \, x^{3} - 2 \, x^{2} + 3 \, x}{\left({7 \, x^{12} + 5 \, x^{2} - 8 \, x + 1}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-5 \, x^{7} - 5 \, x^{2} + 4 \, x + 6}{\left({7 \, x^{10} - 4 \, x^{2} - 2 \, x - 7}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-6 \, x^{3} - 9 \, x^{2} - 6 \, x + 4}{\left({8 \, x^{9} + 7 \, x^{2} - x + 8}\right)^{\frac{1}{3}}} 
= \answer{-3}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{2 \, x^{4} - 3 \, x^{2} - 9 \, x - 8}{\left({8 \, x^{35} + 6 \, x^{2} - 8 \, x - 10}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{3 \, x^{7} + 9 \, x^{2} - 2 \, x + 5}{\left({3 \, x^{21} + x^{2} + x}\right)^{\frac{1}{3}}} 
= \answer{3^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-5 \, x^{4} + 8 \, x^{2} + 4 \, x + 9}{\left({6 \, x^{21} + 4 \, x^{2} + 6 \, x - 1}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{9 \, x^{2} - 5 \, x + 1}{\left({x^{16} + 5 \, x^{2} - 5 \, x + 3}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{6} - x^{2} - 4 \, x}{\left({4 \, x^{18} - 6 \, x^{2} + 6 \, x - 2}\right)^{\frac{1}{3}}} 
= \answer{\frac{7}{4} \cdot 4^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{5 \, x^{6} + 7 \, x^{2} + 4 \, x + 6}{\left({7 \, x^{12} - 2 \, x^{2} + 9 \, x - 9}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-5 \, x^{7} + 9 \, x^{2} + 5 \, x - 1}{\left({8 \, x^{15} - 4 \, x^{2} + 7 \, x - 5}\right)^{\frac{1}{5}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-9 \, x^{7} - 3 \, x^{2} + 4 \, x + 8}{\left({6 \, x^{12} - 9 \, x^{2} + 2 \, x - 8}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{2 \, x^{3} - 4 \, x^{2} - 4 \, x + 10}{\left({6 \, x^{20} + 9 \, x^{2} - 7 \, x + 1}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{4 \, x^{4} + 8 \, x^{2} + 5 \, x}{\left({3 \, x^{21} + x^{2} - 10 \, x + 9}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-6 \, x^{4} + 4 \, x^{2} - 10 \, x + 4}{\left({6 \, x^{15} + 6 \, x - 9}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-4 \, x^{4} + 2 \, x^{2} - 5 \, x - 10}{\left({8 \, x^{15} - 10 \, x^{2} - x + 2}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{6 \, x^{5} + 3 \, x + 4}{\left({9 \, x^{12} - 7 \, x^{2} - 9 \, x - 7}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{5} - 4 \, x^{2} + 6 \, x + 8}{\left({10 \, x^{24} + 3 \, x^{2} + 1}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{5 \, x^{7} - 5 \, x^{2} + 6 \, x - 2}{\left({5 \, x^{25} - 7 \, x^{2} + 3 \, x - 5}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{2 \, x^{6} - 10 \, x^{2} + 6 \, x - 5}{\left({10 \, x^{20} + 7 \, x^{2} + 9 \, x - 9}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-2 \, x^{6} - 8 \, x^{2} - 9 \, x + 10}{\left({2 \, x^{28} - x^{2} - 5 \, x - 3}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{8 \, x^{7} + 2 \, x^{2} - 4 \, x - 4}{\left({4 \, x^{12} - 6 \, x^{2} - 7 \, x + 5}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{3} - x^{2} + 8 \, x + 6}{\left({2 \, x^{35} + 8 \, x + 7}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-3 \, x^{7} + 10 \, x^{2} - 6 \, x + 3}{\left({2 \, x^{35} + 2 \, x^{2} + 4 \, x + 1}\right)^{\frac{1}{5}}} 
= \answer{-\frac{3}{2} \cdot 2^{\frac{4}{5}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-6 \, x^{2} - 7 \, x + 4}{\left({5 \, x^{10} - 2 \, x^{2} + 8 \, x - 4}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{5 \, x^{4} - 9 \, x^{2} - 4 \, x - 2}{\left({9 \, x^{6} - 3 \, x^{2} - 5 \, x + 5}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-6 \, x^{6} - 9 \, x^{2} - 5}{\left({2 \, x^{16} + 9 \, x^{2} - 3 \, x}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-4 \, x^{7} + 7 \, x^{2} - 10 \, x + 7}{\left({10 \, x^{16} + 5 \, x + 10}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-6 \, x^{3} - 6 \, x^{2} + 4 \, x + 9}{\left({5 \, x^{12} - 10 \, x^{2} - x - 3}\right)^{\frac{1}{4}}} 
= \answer{-\frac{6}{5} \cdot 5^{\frac{3}{4}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{x^{3} - 6 \, x^{2} - x - 4}{\left({9 \, x^{18} - 6 \, x^{2} - 5 \, x + 4}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-4 \, x^{5} - 6 \, x^{2} + 6 \, x + 8}{\left({9 \, x^{12} - 9 \, x^{2} + 4 \, x + 8}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{3 \, x^{6} + 9 \, x^{2} - 6 \, x - 10}{\left({5 \, x^{20} - 10 \, x^{2} - 4 \, x + 4}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{7} - 9 \, x^{2} + 9 \, x - 4}{\left({9 \, x^{12} - x^{2} + 7 \, x - 6}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{3 \, x^{5} - 7 \, x^{2} + 1}{\left({5 \, x^{9} - 6 \, x^{2} - 9 \, x}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-4 \, x^{4} - 4 \, x^{2} + 4 \, x + 7}{\left({4 \, x^{14} - 8 \, x^{2} + 10 \, x + 9}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{8 \, x^{4} - 8 \, x^{2} + 10 \, x + 5}{\left({3 \, x^{30} - 7 \, x^{2} - 6 \, x - 10}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{6} + 10 \, x^{2} + 8 \, x - 1}{\left({9 \, x^{6} - 7 \, x^{2} - 4 \, x + 6}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-9 \, x^{7} - 3 \, x^{2} - 8 \, x + 4}{\left({x^{9} + 9 \, x^{2} + 8 \, x + 7}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-3 \, x^{7} + 4 \, x^{2} - 9 \, x + 8}{\left({5 \, x^{16} - 2 \, x^{2} - 8 \, x + 5}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-3 \, x^{7} + 2 \, x^{2} - 4 \, x - 2}{\left({5 \, x^{15} - 8 \, x + 4}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{7 \, x^{3} + x^{2} - 9}{\left({4 \, x^{12} + 7 \, x^{2} - 7 \, x + 10}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{9 \, x^{2} - 7 \, x - 6}{\left({5 \, x^{9} + x^{2} - 4 \, x - 2}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{9 \, x^{5} + 3 \, x - 6}{\left({3 \, x^{18} - 3 \, x^{2} + x - 8}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-x^{3} + 10 \, x^{2} - 10 \, x - 1}{\left({10 \, x^{18} + 9 \, x^{2} - x - 9}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-9 \, x^{7} + 9 \, x^{2} + 2 \, x - 2}{\left({3 \, x^{15} - x^{2} - 8 \, x - 7}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-6 \, x^{5} - x^{2} - 7 \, x + 7}{\left({7 \, x^{12} + 5 \, x^{2} - 8 \, x + 9}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{9 \, x^{4} + 4 \, x^{2} + 8 \, x + 6}{\left({10 \, x^{8} + 2 \, x^{2} - x - 2}\right)^{\frac{1}{2}}} 
= \answer{\frac{9}{10} \, \sqrt{10}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-4 \, x^{6} - 6 \, x^{2} + 5 \, x - 1}{\left({6 \, x^{15} + 5 \, x^{2} - 5 \, x - 9}\right)^{\frac{1}{5}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-x^{6} - 6 \, x^{2} - 2 \, x - 1}{\left({x^{16} - 2 \, x^{2} - 7 \, x - 8}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{9 \, x^{4} - 4 \, x^{2} - 4 \, x - 2}{\left({4 \, x^{12} - 5 \, x^{2} - 7 \, x + 5}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{5} - 6 \, x^{2} - 3 \, x - 8}{\left({x^{15} - 8 \, x^{2} - 9 \, x - 3}\right)^{\frac{1}{3}}} 
= \answer{7}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{x^{3} + 5 \, x^{2} + 2 \, x + 9}{\left({2 \, x^{21} + 9 \, x^{2} + 2 \, x + 3}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{10 \, x^{2} + 7 \, x + 8}{\left({2 \, x^{21} - 9 \, x^{2} + 10 \, x + 8}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-6 \, x^{4} - 8 \, x^{2} + 2 \, x - 9}{\left({5 \, x^{20} + 3 \, x^{2} - 10 \, x - 3}\right)^{\frac{1}{5}}} 
= \answer{-\frac{6}{5} \cdot 5^{\frac{4}{5}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-3 \, x^{7} + 2 \, x^{2} + 7 \, x - 2}{\left({6 \, x^{12} + 9 \, x^{2} + x + 3}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-10 \, x^{5} + 9 \, x^{2} - 9 \, x + 6}{\left({x^{6} + 8 \, x^{2} + 8 \, x + 2}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{3 \, x^{6} + 7 \, x^{2} - 4 \, x}{\left({2 \, x^{12} - 10 \, x^{2} + 7 \, x - 7}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{5 \, x^{4} + 6 \, x^{2} - x - 8}{\left({9 \, x^{24} + 10 \, x^{2} + x + 7}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-6 \, x^{6} + 7 \, x^{2} + 6 \, x - 9}{\left({6 \, x^{25} + 5 \, x^{2} - 6 \, x + 8}\right)^{\frac{1}{5}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{8 \, x^{5} + x^{2} - 4 \, x - 8}{\left({3 \, x^{15} + 8 \, x^{2} + 5 \, x - 2}\right)^{\frac{1}{5}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{7} + 8 \, x^{2} - 10 \, x + 1}{\left({5 \, x^{8} - 5 \, x^{2} - 3 \, x - 7}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{10 \, x^{4} - 5 \, x^{2} + 10 \, x - 3}{\left({9 \, x^{12} - 5 \, x^{2} + 7 \, x + 4}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{4 \, x^{5} - 10 \, x^{2} - 3 \, x - 9}{\left({8 \, x^{30} - 8 \, x^{2} + 2 \, x}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{5} + 2 \, x^{2} - 8 \, x + 8}{\left({3 \, x^{16} + 9 \, x^{2} + 8 \, x + 8}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-3 \, x^{5} - 2 \, x^{2} + 4 \, x + 5}{\left({7 \, x^{10} - 8 \, x^{2} + 4 \, x - 5}\right)^{\frac{1}{2}}} 
= \answer{\frac{3}{7} \, \sqrt{7}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{9 \, x^{3} - 5 \, x^{2} - 8 \, x - 5}{\left({8 \, x^{30} - 10 \, x^{2} + 2 \, x + 5}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{9 \, x^{6} + 9 \, x^{2} + 8 \, x + 2}{\left({6 \, x^{10} + 10 \, x^{2} - 10 \, x + 9}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{5 \, x^{7} + 2 \, x^{2} - 3 \, x - 2}{\left({10 \, x^{24} - 8 \, x^{2} - 5 \, x + 6}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-6 \, x^{5} - 9 \, x^{2} - 5 \, x - 8}{\left({x^{15} - 2 \, x^{2} + 8 \, x}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{5 \, x^{5} + 4 \, x^{2} + 2 \, x + 8}{\left({10 \, x^{14} + 7 \, x^{2} + 6}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{5 \, x^{4} + 10 \, x^{2} - 6 \, x - 1}{\left({6 \, x^{12} + 3 \, x^{2} + 8 \, x - 7}\right)^{\frac{1}{3}}} 
= \answer{\frac{5}{6} \cdot 6^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{6} + 8 \, x^{2} + 10 \, x + 7}{\left({7 \, x^{16} + 4 \, x^{2} - 3 \, x + 6}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-9 \, x^{2} + 10 \, x - 1}{\left({9 \, x^{15} - 10 \, x^{2} - 8 \, x + 2}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-2 \, x^{6} - x^{2} + 7 \, x - 7}{\left({4 \, x^{18} + 8 \, x^{2} + x + 1}\right)^{\frac{1}{3}}} 
= \answer{-\frac{1}{2} \cdot 4^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{3 \, x^{7} - 7 \, x^{2} + 3 \, x + 4}{\left({10 \, x^{8} - 10 \, x - 9}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-2 \, x^{6} - 4 \, x^{2} + 10}{\left({9 \, x^{21} + 5 \, x^{2} - x + 6}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-2 \, x^{3} - 6 \, x - 10}{\left({10 \, x^{9} - x^{2} - 2 \, x - 8}\right)^{\frac{1}{3}}} 
= \answer{-\frac{1}{5} \cdot 10^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-3 \, x^{5} - 3 \, x^{2} + 8 \, x - 8}{\left({10 \, x^{25} - 8 \, x^{2} + 5 \, x - 1}\right)^{\frac{1}{5}}} 
= \answer{-\frac{3}{10} \cdot 10^{\frac{4}{5}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{5} - 10 \, x^{2} - 6 \, x - 2}{\left({7 \, x^{8} - 4 \, x^{2} - 4 \, x + 10}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-8 \, x^{5} + 9 \, x^{2} + 6 \, x - 7}{\left({4 \, x^{12} - 6 \, x^{2} - x + 5}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{5 \, x^{7} - 4 \, x - 7}{\left({8 \, x^{30} - 8 \, x^{2} - 8 \, x - 7}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{3 \, x^{6} + 5 \, x^{2} - 4 \, x + 10}{\left({6 \, x^{24} - 5 \, x^{2} + 4 \, x + 9}\right)^{\frac{1}{4}}} 
= \answer{\frac{1}{2} \cdot 6^{\frac{3}{4}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-10 \, x^{3} + 2 \, x^{2} - 4 \, x + 4}{\left({x^{24} + 3 \, x^{2} - 6 \, x - 9}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{5 \, x^{4} + 8 \, x^{2} - 5 \, x + 9}{\left({4 \, x^{16} - 8 \, x^{2} - 2 \, x + 10}\right)^{\frac{1}{4}}} 
= \answer{\frac{5}{2} \, \sqrt{2}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-x^{3} + 10 \, x^{2} - 2}{\left({8 \, x^{12} + 4 \, x^{2} - 9 \, x - 4}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-6 \, x + 1}{\left({8 \, x^{21} - 9 \, x^{2} + 6 \, x + 8}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{8 \, x^{3} - x^{2} - 2 \, x + 1}{\left({2 \, x^{18} + 6 \, x^{2} - 8 \, x + 10}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{9 \, x^{6} + 6 \, x^{2} - 2 \, x - 6}{\left({6 \, x^{21} - 4 \, x^{2} + 2 \, x - 6}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-7 \, x^{7} - 3 \, x^{2} + 4 \, x + 5}{\left({9 \, x^{18} + 2 \, x^{2} + 4 \, x + 2}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{2 \, x^{6} + 2 \, x^{2} + 10 \, x - 1}{\left({4 \, x^{18} + 9 \, x^{2} + 9 \, x - 5}\right)^{\frac{1}{3}}} 
= \answer{\frac{1}{2} \cdot 4^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{5 \, x^{5} - 10 \, x - 1}{\left({5 \, x^{28} + 5 \, x^{2} - 6 \, x - 4}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-7 \, x^{4} + 3 \, x^{2} - 5 \, x + 4}{\left({5 \, x^{10} + x^{2} + 3 \, x + 3}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-7 \, x^{4} + x^{2} - 2 \, x - 8}{\left({x^{15} - 3 \, x^{2} + 2 \, x + 8}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-10 \, x^{4} + 2 \, x^{2} + 3 \, x + 10}{\left({5 \, x^{12} - 6 \, x^{2} - 4 \, x + 10}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-8 \, x^{3} + 10 \, x^{2} - 8 \, x + 2}{\left({3 \, x^{15} - 7 \, x^{2} - 2 \, x - 4}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-2 \, x^{4} - 2 \, x^{2} + 6 \, x + 4}{\left({8 \, x^{12} - x^{2} - 5 \, x + 8}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-10 \, x^{6} - x^{2} + 5 \, x - 2}{\left({6 \, x^{35} - 4 \, x^{2} + x - 10}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{10 \, x^{3} - 8 \, x^{2} + 9 \, x - 9}{\left({2 \, x^{35} - 5 \, x^{2} + 2 \, x - 7}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{6 \, x^{3} + 3 \, x^{2} - 9 \, x + 10}{\left({10 \, x^{25} + 8 \, x^{2} - 10 \, x - 4}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-3 \, x^{3} - 8 \, x - 4}{\left({3 \, x^{15} - 6 \, x^{2} - x - 6}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{3 \, x^{5} - 6 \, x^{2} - 8 \, x - 8}{\left({7 \, x^{6} - 7 \, x^{2} + 2 \, x + 1}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-9 \, x^{3} + 10 \, x^{2} - 7 \, x - 6}{\left({3 \, x^{9} - 5 \, x^{2} - x - 10}\right)^{\frac{1}{3}}} 
= \answer{-\left(3 \cdot 3^{\frac{2}{3}}\right)}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-6 \, x^{7} - x^{2} - 5 \, x + 10}{\left({x^{16} + 5 \, x^{2} - 5 \, x - 3}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{6 \, x^{5} - 10 \, x^{2} + 7 \, x - 5}{\left({3 \, x^{12} + 6 \, x^{2} - 5 \, x + 4}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{8 \, x^{7} + 6 \, x + 3}{\left({4 \, x^{21} + 5 \, x^{2} + 2 \, x + 3}\right)^{\frac{1}{3}}} 
= \answer{2 \cdot 4^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{2 \, x^{6} - x^{2} - 8 \, x - 1}{\left({7 \, x^{12} - 6 \, x^{2} + 5 \, x - 4}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-7 \, x^{7} + 2 \, x^{2} - 4 \, x - 7}{\left({2 \, x^{15} - 3 \, x^{2} + x - 9}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{7 \, x^{5} - 7 \, x^{2} - x + 8}{\left({x^{14} + 2 \, x^{2} + 9 \, x - 6}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{3 \, x^{6} + 7 \, x^{2} + x + 7}{\left({3 \, x^{12} - 6 \, x^{2} + 8 \, x - 5}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{x^{6} - 10 \, x^{2} - 7 \, x - 1}{\left({3 \, x^{12} + 10 \, x^{2} - 2 \, x + 9}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{4 \, x^{7} + 8 \, x^{2} - 5 \, x - 8}{\left({6 \, x^{25} + 4 \, x^{2} - 6 \, x - 8}\right)^{\frac{1}{5}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{6} + 9 \, x^{2} - x + 3}{\left({6 \, x^{8} + 4 \, x^{2} + 3}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{9 \, x^{5} - 8 \, x^{2} - 5 \, x + 3}{\left({4 \, x^{10} + 4 \, x^{2} - 6 \, x + 9}\right)^{\frac{1}{2}}} 
= \answer{\frac{9}{2}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-10 \, x^{6} - x^{2} - 4 \, x + 3}{\left({x^{30} - 7 \, x^{2} - 4 \, x - 1}\right)^{\frac{1}{5}}} 
= \answer{-10}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-2 \, x^{3} + 7 \, x^{2} + 10 \, x - 7}{\left({5 \, x^{35} - 2 \, x^{2} + 4 \, x + 4}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{9 \, x^{6} - 5 \, x^{2} - 2 \, x - 3}{\left({6 \, x^{15} - 7 \, x^{2} - x + 1}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{6 \, x^{4} + 8 \, x^{2} + 7 \, x + 4}{\left({5 \, x^{20} + 2 \, x^{2} - 5 \, x + 10}\right)^{\frac{1}{5}}} 
= \answer{\frac{6}{5} \cdot 5^{\frac{4}{5}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{5 \, x^{2} - 10 \, x + 1}{\left({5 \, x^{10} - 2 \, x^{2} + 5 \, x + 8}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-x^{6} + 7 \, x^{2} - 10 \, x + 8}{\left({5 \, x^{21} - 10 \, x^{2} - x - 6}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{2 \, x^{3} - 7 \, x^{2} - x - 9}{\left({9 \, x^{28} - 10 \, x^{2} + 9}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-4 \, x^{3} + 4 \, x^{2} + 6 \, x - 10}{\left({5 \, x^{18} + 9 \, x^{2} - 7 \, x - 4}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-9 \, x^{4} + 4 \, x^{2} - 8 \, x + 3}{\left({9 \, x^{28} - 8 \, x^{2} - 3 \, x + 3}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{5 \, x^{4} - 5 \, x^{2} - 2 \, x + 7}{\left({7 \, x^{12} + 4 \, x^{2} - 6 \, x - 10}\right)^{\frac{1}{3}}} 
= \answer{\frac{5}{7} \cdot 7^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-8 \, x^{4} - 10 \, x^{2} - 10 \, x + 9}{\left({4 \, x^{10} - 5 \, x + 10}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-3 \, x^{4} - 8 \, x^{2} + 8 \, x - 7}{\left({10 \, x^{15} + 6 \, x^{2} - 10 \, x}\right)^{\frac{1}{5}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{9 \, x^{6} - 5 \, x^{2} + 8 \, x + 3}{\left({3 \, x^{15} + x^{2} - 7 \, x - 7}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-10 \, x^{4} + 2 \, x^{2} + 5 \, x - 3}{\left({x^{30} + 10 \, x^{2} - x - 2}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{5 \, x^{7} + 3 \, x^{2} + 4 \, x + 7}{\left({9 \, x^{20} + 5 \, x^{2} - 8 \, x - 9}\right)^{\frac{1}{5}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{2 \, x^{3} + 10 \, x^{2} + x + 9}{\left({5 \, x^{16} + 6 \, x^{2} + x}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{6 \, x^{3} + 3 \, x^{2} - 7 \, x - 9}{\left({9 \, x^{15} - 7 \, x^{2} - 7 \, x - 4}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{3} + 5 \, x^{2} - 3 \, x - 2}{\left({10 \, x^{14} - 4 \, x^{2} + x - 4}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{9 \, x^{6} + 4 \, x^{2} + 4 \, x - 2}{\left({7 \, x^{14} + 8 \, x^{2} + 2 \, x + 10}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{5 \, x^{5} - 4 \, x^{2} - 7 \, x - 7}{\left({7 \, x^{28} + 8 \, x^{2} - 2 \, x}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{3 \, x^{5} - 9 \, x^{2} - 10 \, x + 8}{\left({6 \, x^{12} + 9 \, x^{2} - 10 \, x + 2}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{6 \, x^{3} - 3 \, x^{2} + 7 \, x + 4}{\left({9 \, x^{12} - x^{2} + 9 \, x + 5}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-9 \, x^{4} - 7 \, x^{2} + 3 \, x - 3}{\left({5 \, x^{6} + 5 \, x^{2} + 5 \, x + 4}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-2 \, x^{6} - 8 \, x^{2} - 5 \, x - 1}{\left({2 \, x^{10} + x^{2} + 4 \, x + 7}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{x^{2} + 5 \, x + 10}{\left({9 \, x^{35} + 3 \, x^{2} + x - 5}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-7 \, x^{4} - 9 \, x^{2} - 2 \, x + 10}{\left({3 \, x^{12} - 8 \, x^{2} + 4 \, x - 2}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{7 \, x^{4} - 10 \, x^{2} + 7 \, x + 5}{\left({x^{28} - x^{2} + 6 \, x - 4}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-4 \, x^{2} - 4 \, x - 9}{\left({7 \, x^{24} + 3 \, x^{2} - 1}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-5 \, x^{7} + 8 \, x^{2} + 8 \, x + 9}{\left({4 \, x^{12} + x^{2} - 7 \, x + 2}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-8 \, x^{4} - x^{2} - 8 \, x - 3}{\left({4 \, x^{35} - x^{2} - 3 \, x + 9}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{4 \, x^{6} - x^{2} - 2 \, x - 10}{\left({3 \, x^{16} - 4 \, x^{2} + x - 1}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{9 \, x^{7} - 5 \, x^{2} + 8 \, x + 6}{\left({10 \, x^{8} - 3 \, x^{2} - 5 \, x + 3}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{5} - 9 \, x + 3}{\left({8 \, x^{14} + 7 \, x^{2} + 9 \, x - 2}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-7 \, x^{4} - 4 \, x^{2} + 6 \, x + 9}{\left({7 \, x^{28} + 6 \, x^{2} + 8 \, x + 5}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-3 \, x - 8}{\left({x^{16} - 5 \, x^{2} + 7 \, x + 2}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{6 \, x^{4} - 2 \, x^{2} - 9 \, x - 1}{\left({9 \, x^{24} + 2 \, x^{2} + 3 \, x - 5}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{3 \, x^{3} + 8 \, x^{2} - 6 \, x + 10}{\left({7 \, x^{10} - 4 \, x^{2} + 2 \, x - 7}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{9 \, x^{7} - 3 \, x^{2} - 5}{\left({8 \, x^{18} + 5 \, x^{2} - 9 \, x + 9}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{4 \, x^{7} - 4 \, x^{2} + 5 \, x - 6}{\left({6 \, x^{15} - x^{2} - 10 \, x + 6}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-3 \, x^{3} + 2 \, x^{2} - 8 \, x - 8}{\left({2 \, x^{12} + 9 \, x^{2} + 10 \, x + 8}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{10 \, x^{5} - 4 \, x^{2} + 9 \, x - 8}{\left({x^{6} + 9 \, x^{2} - 3 \, x - 4}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-x^{5} - 9 \, x^{2} + 2 \, x - 10}{\left({3 \, x^{14} + 2 \, x^{2} + 5 \, x - 4}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{2} - 5 \, x - 9}{\left({9 \, x^{24} - 3 \, x^{2} + 9 \, x + 1}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{3 \, x^{4} + 10 \, x^{2} - 9 \, x + 10}{\left({x^{20} + 8 \, x - 2}\right)^{\frac{1}{5}}} 
= \answer{3}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-6 \, x^{3} + 3 \, x^{2} - 2 \, x + 9}{\left({5 \, x^{35} - 8 \, x^{2} - 10 \, x - 9}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-x^{3} + 8 \, x^{2} - 5 \, x - 3}{\left({8 \, x^{14} + 2 \, x^{2} + 4 \, x + 2}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{x^{6} - 6 \, x^{2} - 5 \, x + 10}{\left({10 \, x^{12} + 4 \, x^{2} + 6 \, x + 7}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{2 \, x^{7} + x^{2} - 7 \, x + 6}{\left({2 \, x^{28} + 4 \, x^{2} + 2}\right)^{\frac{1}{4}}} 
= \answer{2^{\frac{3}{4}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{8 \, x^{5} - 5 \, x^{2} + 3 \, x + 10}{\left({5 \, x^{12} + 7 \, x^{2} - 10 \, x - 5}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{4 \, x^{3} + 2 \, x^{2} + 5 \, x}{\left({10 \, x^{12} - 9 \, x^{2} - 2 \, x + 1}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-3 \, x^{4} - 7 \, x^{2} - x}{\left({9 \, x^{21} + 3 \, x^{2} - 10 \, x + 10}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{8 \, x^{7} + 3 \, x^{2} + 10 \, x - 9}{\left({8 \, x^{9} + 8 \, x^{2} - 2 \, x - 6}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-6 \, x^{7} - 5 \, x - 3}{\left({2 \, x^{12} + 10 \, x^{2} - 2 \, x - 5}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{5 \, x^{4} + 9 \, x^{2} - 2 \, x - 10}{\left({10 \, x^{15} + 8 \, x^{2} - 10 \, x + 6}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-9 \, x^{2} + x - 8}{\left({4 \, x^{12} + x^{2} + x + 3}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-5 \, x^{7} + 3 \, x^{2} + 4 \, x - 1}{\left({x^{30} + 10 \, x^{2} - 6 \, x - 8}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-x^{4} - x^{2} - 7 \, x + 6}{\left({3 \, x^{10} - 6 \, x^{2} + 2 \, x + 6}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{8 \, x^{7} - 3 \, x^{2} + x + 8}{\left({9 \, x^{8} + 10 \, x^{2} + 3 \, x}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{x^{6} + 7 \, x^{2} - 7 \, x - 6}{\left({2 \, x^{16} + 4 \, x^{2} + 8 \, x + 5}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{8 \, x^{6} + 7 \, x^{2} + 7 \, x - 8}{\left({2 \, x^{12} + 6 \, x^{2} + 4 \, x + 4}\right)^{\frac{1}{2}}} 
= \answer{2^{\frac{5}{2}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-10 \, x^{6} - 4 \, x^{2} + 8 \, x - 5}{\left({6 \, x^{10} + 5 \, x + 7}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-9 \, x^{3} - 10 \, x^{2} + 10 \, x - 1}{\left({7 \, x^{18} - x^{2} + 8 \, x - 10}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{9 \, x^{4} + x^{2} - 9 \, x - 8}{\left({5 \, x^{35} + x^{2} + 5 \, x - 2}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{3} + 2 \, x^{2} - 3 \, x + 3}{\left({10 \, x^{15} - 2 \, x^{2} + 2 \, x + 5}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-10 \, x^{4} - 5 \, x^{2} - 8 \, x + 6}{\left({7 \, x^{18} - 4 \, x^{2} - 2 \, x - 8}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{3 \, x^{7} + 5 \, x^{2} + 1}{\left({7 \, x^{10} + 6 \, x^{2} + 4 \, x - 10}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-8 \, x^{5} + 10 \, x^{2} - 3 \, x + 10}{\left({x^{21} + 7 \, x^{2} + 4 \, x + 9}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-9 \, x^{6} + 10 \, x^{2} + 7}{\left({7 \, x^{9} - 10 \, x^{2} + 4 \, x}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-7 \, x^{4} - 6 \, x^{2} + 4 \, x - 8}{\left({8 \, x^{35} + 8 \, x^{2} + 6 \, x - 10}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-10 \, x^{6} - 3 \, x^{2} + 10 \, x - 1}{\left({9 \, x^{20} - 10 \, x^{2} + 10 \, x + 10}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-x^{3} - 2 \, x^{2} + 2 \, x - 4}{\left({8 \, x^{24} - 10 \, x^{2} + x - 1}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-8 \, x^{5} + 5 \, x - 6}{\left({9 \, x^{10} + 3 \, x^{2} - 3 \, x + 2}\right)^{\frac{1}{2}}} 
= \answer{\frac{8}{3}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{8 \, x^{5} + 2 \, x^{2} + x - 3}{\left({9 \, x^{20} + 2 \, x^{2} + 3 \, x + 1}\right)^{\frac{1}{4}}} 
= \answer{-\frac{8}{3} \, \sqrt{3}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-3 \, x^{4} + 8 \, x^{2} - 5 \, x + 2}{\left({10 \, x^{14} - 3 \, x^{2} - 7 \, x + 4}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-5 \, x^{5} + x^{2} + 2 \, x + 5}{\left({10 \, x^{20} + 4 \, x^{2} - 2 \, x + 8}\right)^{\frac{1}{5}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-x^{4} - 9 \, x^{2} + 7 \, x + 4}{\left({x^{24} + 7 \, x^{2} - 3 \, x - 9}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{5 \, x^{4} + 8 \, x^{2} + x + 5}{\left({5 \, x^{12} - 4 \, x^{2} - x - 8}\right)^{\frac{1}{3}}} 
= \answer{5^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-8 \, x^{6} - 6 \, x^{2} - 4 \, x - 6}{\left({2 \, x^{15} + 8 \, x^{2} - 5 \, x - 7}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{2 \, x^{5} - 6 \, x^{2} - 6 \, x + 9}{\left({10 \, x^{15} + 7 \, x^{2} + 7}\right)^{\frac{1}{3}}} 
= \answer{\frac{1}{5} \cdot 10^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-2 \, x^{7} + 3 \, x - 4}{\left({x^{12} + 6 \, x^{2} - 1}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-9 \, x^{7} - 10 \, x^{2} + 4 \, x + 10}{\left({4 \, x^{15} - 5 \, x^{2} + 3 \, x + 4}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{3 \, x^{4} + 2 \, x^{2} + 9 \, x - 4}{\left({2 \, x^{24} + 2 \, x^{2} + 7 \, x - 2}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-2 \, x^{6} + 8 \, x^{2} - 5 \, x + 2}{\left({x^{15} - 4 \, x^{2} + 4 \, x - 8}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{8 \, x^{7} + 4 \, x^{2} - 7 \, x + 3}{\left({x^{21} + 4 \, x^{2} - 4 \, x - 1}\right)^{\frac{1}{3}}} 
= \answer{8}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{5} - 7 \, x^{2} + 2 \, x - 8}{\left({x^{12} + 7 \, x^{2} + 9 \, x - 2}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-5 \, x^{5} + 6 \, x^{2} + 2 \, x - 3}{\left({9 \, x^{14} + 9 \, x^{2} - 10 \, x - 5}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{8 \, x^{5} - 3 \, x^{2} + 2 \, x + 8}{\left({8 \, x^{28} - 10 \, x^{2} - x - 3}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-7 \, x^{3} + 5 \, x^{2} - 3 \, x - 8}{\left({6 \, x^{12} + 10 \, x^{2} + 10 \, x + 10}\right)^{\frac{1}{4}}} 
= \answer{\frac{7}{6} \cdot 6^{\frac{3}{4}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{10 \, x^{7} - 5 \, x^{2} + 7 \, x - 2}{\left({6 \, x^{21} + 9 \, x^{2} - 10 \, x + 7}\right)^{\frac{1}{3}}} 
= \answer{\frac{5}{3} \cdot 6^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{3 \, x^{4} + 10 \, x^{2} + 6 \, x - 9}{\left({10 \, x^{25} + 4 \, x^{2} + x - 4}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-3 \, x^{5} - 10 \, x^{2} - 3 \, x - 7}{\left({2 \, x^{8} + x^{2} + 4 \, x + 3}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{7 \, x^{5} - x^{2} - 6 \, x - 7}{\left({3 \, x^{6} + 8 \, x^{2} - 4 \, x - 4}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{x^{2} + 8 \, x - 5}{\left({3 \, x^{20} - 4 \, x^{2} + 10 \, x + 2}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-7 \, x^{5} - 3 \, x^{2} - 3 \, x + 10}{\left({8 \, x^{24} - 2 \, x^{2} - 7 \, x + 8}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{4 \, x^{6} + 2 \, x^{2} + 7 \, x - 8}{\left({7 \, x^{20} + 7 \, x^{2} + 3 \, x + 7}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{2 \, x^{3} - 9 \, x^{2} - 2 \, x}{\left({3 \, x^{6} - 7 \, x^{2} - 3 \, x - 5}\right)^{\frac{1}{2}}} 
= \answer{\frac{2}{3} \, \sqrt{3}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-3 \, x^{7} + 3 \, x^{2} - x - 8}{\left({7 \, x^{25} + 3 \, x^{2} + 9 \, x}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-2 \, x^{7} + 4 \, x^{2} + 2 \, x}{\left({8 \, x^{15} - 8 \, x^{2} - 5 \, x - 3}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{x^{6} - 5 \, x^{2} - 2 \, x - 9}{\left({2 \, x^{20} - 10 \, x^{2} + 10 \, x + 5}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-7 \, x^{4} - 3 \, x^{2} + 6 \, x}{\left({3 \, x^{20} - 10 \, x^{2} + 4}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{10 \, x^{2} - x - 1}{\left({3 \, x^{35} - x^{2} + 3 \, x + 10}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-6 \, x^{5} - 4 \, x^{2} + 3 \, x + 6}{\left({5 \, x^{20} + 8 \, x^{2} + 2 \, x - 10}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{4 \, x^{3} + 9 \, x^{2} + 2 \, x + 5}{\left({3 \, x^{25} - 4 \, x^{2} + 9 \, x - 3}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-9 \, x^{7} + 7 \, x^{2} + 6 \, x + 3}{\left({x^{15} + 10 \, x^{2} + 4 \, x - 5}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-2 \, x^{7} + 7 \, x^{2} + 9 \, x - 5}{\left({x^{12} - x^{2} - 5 \, x + 9}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{10 \, x^{5} - 6 \, x^{2} - 2 \, x - 5}{\left({5 \, x^{12} - x^{2} - 9 \, x + 2}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{6 \, x^{3} - 2 \, x^{2} - 5 \, x - 7}{\left({8 \, x^{20} + 3 \, x^{2} - 4 \, x - 10}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-4 \, x^{6} + 2 \, x^{2} - 8 \, x + 1}{\left({6 \, x^{28} + 4 \, x^{2} - 6 \, x + 5}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-5 \, x^{4} - 2 \, x^{2} - 5 \, x - 7}{\left({3 \, x^{35} - 5 \, x^{2} - 5 \, x - 2}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{6 \, x^{6} - 2 \, x^{2} + 5 \, x + 10}{\left({8 \, x^{25} + 5 \, x^{2} + 6 \, x + 6}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{8 \, x^{6} - 10 \, x^{2} + 2 \, x + 1}{\left({5 \, x^{18} + 9 \, x^{2} - 2 \, x - 5}\right)^{\frac{1}{3}}} 
= \answer{\frac{8}{5} \cdot 5^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{7} + 10 \, x^{2} + 8 \, x - 5}{\left({7 \, x^{35} + 4 \, x^{2} - 10 \, x + 9}\right)^{\frac{1}{5}}} 
= \answer{\frac{6}{7} \cdot 7^{\frac{4}{5}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-x^{6} + x^{2} + x - 2}{\left({8 \, x^{16} + x^{2} - 5 \, x - 1}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{7} - 8 \, x^{2} + 8 \, x - 8}{\left({10 \, x^{9} + 3 \, x^{2} + 7 \, x - 2}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{8 \, x^{7} + x^{2} - 3 \, x}{\left({4 \, x^{20} - 8 \, x^{2} - 6 \, x - 10}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-9 \, x^{3} - 7 \, x^{2} + 10 \, x - 9}{\left({4 \, x^{8} - 4 \, x^{2} - 10 \, x + 2}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-8 \, x^{6} - 3 \, x^{2} + 4 \, x - 9}{\left({9 \, x^{12} + 7 \, x^{2} - 3 \, x + 3}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-8 \, x^{5} + 3 \, x^{2} - 5 \, x + 3}{\left({2 \, x^{20} - 10 \, x^{2} + 7 \, x}\right)^{\frac{1}{4}}} 
= \answer{-\left(4 \cdot 2^{\frac{3}{4}}\right)}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{7 \, x^{3} + 8 \, x^{2} + 7 \, x - 2}{\left({4 \, x^{30} + 5 \, x^{2} - 8 \, x - 6}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{10 \, x^{7} + 6 \, x^{2} + 7 \, x + 8}{\left({2 \, x^{16} + 3 \, x^{2} - 6 \, x + 4}\right)^{\frac{1}{4}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{10 \, x^{7} - 5 \, x^{2} - 6 \, x - 4}{\left({5 \, x^{24} + 8 \, x^{2} + 7 \, x + 10}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-4 \, x^{5} - 5 \, x^{2} + 7 \, x}{\left({5 \, x^{24} + 4 \, x^{2} - 6 \, x + 1}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{6 \, x^{6} - 10 \, x^{2} + 6 \, x}{\left({9 \, x^{15} - 6 \, x^{2} + 8 \, x - 9}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-3 \, x^{7} + 4 \, x^{2} - 4 \, x + 1}{\left({6 \, x^{9} + 2 \, x^{2} + 4}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{3 \, x^{3} - 9 \, x^{2} + 7 \, x - 7}{\left({5 \, x^{15} - 8 \, x^{2} + 8 \, x - 10}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-9 \, x^{5} - 5 \, x^{2} + 3 \, x - 10}{\left({4 \, x^{18} - 10 \, x^{2} + 3 \, x + 8}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{10 \, x^{7} + 8 \, x^{2} - 2 \, x - 10}{\left({x^{21} - 8 \, x^{2} - 9 \, x + 2}\right)^{\frac{1}{3}}} 
= \answer{10}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-7 \, x^{6} + 4 \, x^{2} + x - 6}{\left({5 \, x^{14} - 8 \, x^{2} + 10 \, x + 10}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{9 \, x^{3} + 4 \, x^{2} + 8 \, x - 8}{\left({3 \, x^{28} + 2 \, x^{2} + 7 \, x - 2}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-6 \, x^{5} - 3 \, x^{2} + 10 \, x + 10}{\left({9 \, x^{30} + 10 \, x^{2} + 8 \, x + 6}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{3 \, x^{3} - 10 \, x^{2} - 7 \, x}{\left({8 \, x^{12} - 3 \, x^{2} + 10 \, x + 1}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{8 \, x^{5} - 4 \, x^{2} + 6 \, x + 10}{\left({9 \, x^{15} - 8 \, x^{2} + 7 \, x + 10}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-7 \, x^{5} + x^{2} + 9 \, x - 7}{\left({x^{35} - 9 \, x^{2} + 2 \, x + 5}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{5 \, x^{2} + 10}{\left({10 \, x^{9} - x^{2} + x - 9}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-7 \, x^{4} - 4 \, x^{2} + 9 \, x - 9}{\left({10 \, x^{12} - 10 \, x^{2} - 10 \, x + 1}\right)^{\frac{1}{3}}} 
= \answer{-\frac{7}{10} \cdot 10^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-7 \, x^{6} + 4 \, x^{2} + 8 \, x - 6}{\left({4 \, x^{18} + 5 \, x^{2} + x + 9}\right)^{\frac{1}{3}}} 
= \answer{-\frac{7}{4} \cdot 4^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-3 \, x^{5} + x^{2} + 6 \, x}{\left({4 \, x^{20} + 3 \, x^{2} - 3 \, x - 9}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-2 \, x^{2} + 2 \, x - 6}{\left({10 \, x^{12} - 8 \, x^{2} - 9 \, x + 8}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{8 \, x^{7} + 9 \, x^{2} + x - 6}{\left({x^{14} - 6 \, x^{2} + 9 \, x + 10}\right)^{\frac{1}{2}}} 
= \answer{8}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-4 \, x^{5} + 7 \, x^{2} + 7 \, x - 9}{\left({2 \, x^{20} - 4 \, x^{2} - 3 \, x + 1}\right)^{\frac{1}{4}}} 
= \answer{-\left(2 \cdot 2^{\frac{3}{4}}\right)}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-6 \, x^{3} + 9 \, x^{2} + 2 \, x}{\left({6 \, x^{12} + 5 \, x^{2} + 8 \, x}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-10 \, x^{6} + x^{2} - 6 \, x - 4}{\left({10 \, x^{10} + x^{2} + 8 \, x + 1}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{7 \, x^{4} - 4 \, x^{2} + 4 \, x + 9}{\left({10 \, x^{6} + 2 \, x^{2} + 4 \, x + 4}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{4 \, x^{3} - x^{2} - x - 5}{\left({x^{28} - 4 \, x^{2} - 5 \, x - 8}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{4 \, x^{2} + 7 \, x - 7}{\left({x^{10} - 4 \, x^{2} + 9 \, x - 1}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{3 \, x^{2} - 2 \, x - 10}{\left({x^{15} - 2 \, x^{2} - 7 \, x - 7}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{3 \, x^{7} + x^{2} - 7 \, x - 2}{\left({7 \, x^{15} + 6 \, x^{2} + 2 \, x + 10}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{9 \, x^{7} + 6 \, x^{2} + 9 \, x - 10}{\left({8 \, x^{15} + 6 \, x^{2} - 4 \, x + 8}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-4 \, x^{3} - 7 \, x + 3}{\left({5 \, x^{28} - 5 \, x^{2} - x + 10}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{5 \, x^{7} - x^{2} + x - 5}{\left({3 \, x^{8} - 10 \, x^{2} + 2 \, x - 8}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-8 \, x^{6} + 5 \, x^{2} + 10 \, x}{\left({10 \, x^{20} - 6 \, x^{2} + 4 \, x + 5}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{5 \, x^{4} + x^{2} - 10 \, x - 5}{\left({10 \, x^{15} + 4 \, x^{2} - 10 \, x + 4}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-2 \, x^{7} - x^{2} - 9 \, x + 7}{\left({7 \, x^{15} - 6 \, x^{2} - 6 \, x + 3}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-3 \, x^{5} - 9 \, x^{2} + 4 \, x - 4}{\left({2 \, x^{15} + 10 \, x^{2} - x - 3}\right)^{\frac{1}{3}}} 
= \answer{-\frac{3}{2} \cdot 2^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{10 \, x^{6} - 4 \, x^{2} + 9 \, x + 9}{\left({10 \, x^{15} + 3 \, x^{2} + 10 \, x}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{x^{4} + 3 \, x^{2} + 4 \, x}{\left({8 \, x^{8} - 6 \, x^{2} - 5 \, x + 7}\right)^{\frac{1}{2}}} 
= \answer{\frac{1}{4} \, \sqrt{2}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-6 \, x^{6} + 8 \, x + 2}{\left({4 \, x^{8} - 7 \, x^{2} - 8 \, x}\right)^{\frac{1}{2}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-9 \, x^{4} + x^{2} + 6 \, x - 6}{\left({7 \, x^{14} + 2 \, x^{2} - 10 \, x - 7}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{10 \, x^{4} + 7 \, x^{2} - 10 \, x + 4}{\left({3 \, x^{21} + 6 \, x^{2} - 8 \, x - 1}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-3 \, x^{7} + x^{2} + 7 \, x - 5}{\left({8 \, x^{18} + 8 \, x^{2} - 7 \, x - 6}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{10 \, x^{4} + 5 \, x^{2} + x + 10}{\left({8 \, x^{30} - 3 \, x^{2} - 8 \, x - 7}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-8 \, x^{4} + 9 \, x^{2} - 2 \, x + 3}{\left({8 \, x^{18} + 5 \, x^{2} + 6 \, x}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-9 \, x^{3} - 8 \, x^{2} + 7 \, x - 5}{\left({5 \, x^{6} + x^{2} - 2 \, x - 9}\right)^{\frac{1}{2}}} 
= \answer{\frac{9}{5} \, \sqrt{5}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-8 \, x^{3} - 4 \, x^{2} - x + 1}{\left({5 \, x^{12} - 9 \, x^{2} - 8 \, x + 8}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{6 \, x^{6} + 8 \, x + 1}{\left({9 \, x^{12} + 6 \, x^{2} - 6 \, x - 9}\right)^{\frac{1}{2}}} 
= \answer{2}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{7 \, x^{4} - 6 \, x^{2} - 10 \, x + 9}{\left({5 \, x^{24} - 2 \, x^{2} + 2 \, x + 9}\right)^{\frac{1}{4}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-4 \, x^{4} - 10 \, x^{2} + 2 \, x - 1}{\left({3 \, x^{9} - 8 \, x^{2} + 9}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-9 \, x^{7} + 6 \, x^{2} - 4 \, x + 4}{\left({3 \, x^{15} + 3 \, x^{2} - 6 \, x + 3}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-10 \, x^{5} + 5 \, x^{2} - 10 \, x - 5}{\left({4 \, x^{9} + 4 \, x^{2} - 6 \, x - 5}\right)^{\frac{1}{3}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-6 \, x^{4} - 2 \, x^{2} - 4 \, x + 10}{\left({4 \, x^{21} + x^{2} + x + 6}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-5 \, x^{3} + 4 \, x^{2} + 2 \, x - 6}{\left({7 \, x^{12} - x^{2} - 1}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-9 \, x^{7} - 8 \, x^{2} - x - 10}{\left({3 \, x^{16} - 9 \, x^{2} - 2 \, x + 9}\right)^{\frac{1}{4}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{4 \, x^{5} + 7 \, x^{2} - 5 \, x + 7}{\left({3 \, x^{9} - x^{2} - 8 \, x + 6}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{3 \, x^{3} + 4 \, x^{2} + 2 \, x - 8}{\left({3 \, x^{9} + 7 \, x^{2} + 2 \, x - 3}\right)^{\frac{1}{3}}} 
= \answer{3^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{9 \, x^{5} + 7 \, x^{2} - x + 7}{\left({10 \, x^{15} - 8 \, x^{2} - 3 \, x + 9}\right)^{\frac{1}{3}}} 
= \answer{\frac{9}{10} \cdot 10^{\frac{2}{3}}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{5 \, x^{6} - 8 \, x^{2} - 2 \, x - 4}{\left({5 \, x^{8} - 8 \, x^{2} + 6 \, x + 2}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{3 \, x^{2} + 2 \, x - 1}{\left({4 \, x^{25} + 2 \, x^{2} + 4 \, x + 7}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{10 \, x^{4} - 6 \, x^{2} + 5 \, x - 8}{\left({8 \, x^{15} + 8 \, x^{2} + 2 \, x - 8}\right)^{\frac{1}{5}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{3 \, x^{6} + 10 \, x^{2} + 3 \, x + 6}{\left({2 \, x^{21} + 3 \, x^{2} + 5 \, x - 4}\right)^{\frac{1}{3}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{x^{7} + 10 \, x^{2} - 7 \, x - 8}{\left({2 \, x^{12} - 10 \, x^{2} + 5 \, x - 2}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{4 \, x^{7} - 8 \, x^{2} - 4 \, x - 8}{\left({7 \, x^{8} + 2 \, x^{2} - 2 \, x}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-6 \, x^{6} - 3 \, x^{2} - 4}{\left({3 \, x^{15} + 9 \, x^{2} + 8 \, x + 7}\right)^{\frac{1}{5}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{3 \, x^{7} + 6 \, x^{2} - 6 \, x + 7}{\left({9 \, x^{18} + 3 \, x^{2} + 9 \, x - 2}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-x^{6} + 6 \, x^{2} + 10}{\left({5 \, x^{9} - 7 \, x^{2} - 6 \, x + 4}\right)^{\frac{1}{3}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-10 \, x^{3} + x^{2} - 9 \, x - 6}{\left({8 \, x^{20} + 9 \, x^{2} + 7 \, x + 3}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-x^{4} + 6 \, x^{2} - 1}{\left({6 \, x^{8} + x^{2} - 6 \, x + 5}\right)^{\frac{1}{2}}} 
= \answer{-\frac{1}{6} \, \sqrt{6}}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{10 \, x^{7} + 6 \, x^{2} - 5 \, x + 2}{\left({8 \, x^{15} - 3 \, x^{2} - 4 \, x + 2}\right)^{\frac{1}{5}}} 
= \answer{-\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{-4 \, x^{6} + 3 \, x^{2} + x - 5}{\left({7 \, x^{35} - 10 \, x^{2} - 10 \, x + 1}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{9 \, x^{3} - 6 \, x^{2} + 4 \, x + 7}{\left({3 \, x^{8} - 6 \, x^{2} + 7 \, x - 10}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{2 \, x^{6} - 8 \, x^{2} + 7 \, x - 6}{\left({x^{8} + 4 \, x^{2} - 10 \, x - 7}\right)^{\frac{1}{2}}} 
= \answer{+\infty}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {-\infty}} \dfrac{10 \, x^{5} + 8 \, x^{2} + 5 \, x + 10}{\left({2 \, x^{30} + 8 \, x^{2} + 10 \, x + 9}\right)^{\frac{1}{5}}} 
= \answer{0}$.
\end{problem}}



\latexProblemContent{
\begin{problem}
Compute the following limit (Hint, rewrite the fractional exponent as a root):

\input{./Limits/2311-Compute-Limit-0021.HELP.tex}

$\lim\limits_{x \rightarrow {+\infty}} \dfrac{-x^{5} - 6 \, x^{2} + 6 \, x + 9}{\left({x^{12} - 7 \, x^{2} - 7 \, x - 10}\right)^{\frac{1}{2}}} 
= \answer{0}$.
\end{problem}}


